
dispatch:

  # To deploy this file... 
  # gcloud config set project ore-id 
  # gcloud app deploy dispatch.yaml

  # Default service serves the typical web resources and all static resources.
  # - url: "*/favicon.ico"
  #   service: web

  # prod - service
- url: "crypto.oreid.io/*"
  service: crypto-service

  # staging - service
- url: "staging.crypto.oreid.io/*"
  service: crypto-service-staging
